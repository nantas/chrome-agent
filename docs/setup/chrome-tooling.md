# Chrome Tooling Setup

This document is the bootstrap note for connecting Chrome tooling into `chrome-agent`.

## Current Intent

- Prepare a place to document how `chrome-devtools-mcp` is installed and validated
- Prepare a place to document how `chrome-cdp-skill` is installed and validated
- Keep the instructions minimal until the first real task confirms the actual workflow

## Known Facts

- Local Chrome has already been confirmed at version `146`
- `chrome-devtools-mcp` should be the default path
- `chrome-cdp-skill` remains available as a supplemental path when live-session reuse matters

## To Be Added After First Validation

- installation steps
- environment assumptions
- minimal connectivity checks
- first-run troubleshooting notes
