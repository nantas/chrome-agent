# CONTEXT-MAP.md — chrome-agent 模块关系图

> 本文件描述项目模块之间的依赖和通信关系。`improve-codebase-architecture` 和 `grill-with-docs` skill 会读取此文件来理解架构约束。

## 模块总览

```
┌─────────────────────────────────────────────┐
│                  chrome-agent                │
│                                             │
│  ┌─────────┐   ┌──────────┐   ┌──────────┐ │
│  │  CLI    │──▶│ Pipeline │──▶│  Output  │ │
│  │ (mjs)   │   │ (python) │   │          │ │
│  └─────────┘   └──────────┘   └──────────┘ │
│       │              │                      │
│       ▼              ▼                      │
│  ┌─────────┐   ┌──────────┐                │
│  │ Explore │   │ Engine   │                │
│  │         │   │ Registry │                │
│  └─────────┘   └──────────┘                │
│       │                                     │
│       ▼                                     │
│  ┌─────────────┐                            │
│  │ Strategies  │                            │
│  │ (frontmatter)│                           │
│  └─────────────┘                            │
└─────────────────────────────────────────────┘
```

## 依赖方向

| 上游 → 下游 | 依赖类型 | 备注 |
|-------------|---------|------|
| _待填充_ | | |

## 共享内核（Shared Kernel）

| 组件 | 位置 | 被谁使用 |
|------|------|---------|
| _待填充_ | | |

## 反腐败层（Anti-Corruption Layer）

| 边界 | 隔离方式 | 备注 |
|------|---------|------|
| _待填充_ | | |

---

_此文件由 `$setup-matt-pocock-skills` 创建。后续通过 `improve-codebase-architecture` skill 增量填充。_
